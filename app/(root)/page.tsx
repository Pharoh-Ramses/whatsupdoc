import { Button } from "@/components/ui/button";
import React from "react";

type Props = {};

const Home = (props: Props) => {
    return (
        <div>
            <Button>Hello</Button>
        </div>
    );
};

export default Home;
